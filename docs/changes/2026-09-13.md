# 2026-09-13 变更记录（多季归错季 + 媒体库交互与展示 + 文档结构）

> 编号格式 `2026-09-13 #n`；四段式：**现象 → 原因 → 做法 → 验证**。
> 本文档结构整理（把变更记录从 `需求与设计文档.md` 正文迁出）见 #12。

---

## 一、归档正确性

### #1 多季作品被归到别的季 —— 别名表脏数据（在线检索没有纠正机会）

- **现象**：`[ANi] 小書痴的下剋上 為了成為圖書管理員不擇手段！領主的養女 - 21`（第四季）
  被自动归到**第一季**文件夹 `小书痴的下克上 〜为了成为图书管理员而不择手段〜`（2019/10）。
  （用户 09-12 反馈；日志还原：15:39 那批还是对的 → **15:45 脏记录被写入** → 15:55 那一集归错。）
- **原因**：别名表里有 6 条旧写入的**脏记录**（第一季条目把第四季的名字当成了自己的别名），
  且 `src=manual`、置信度 **1.0** ⇒ `lookup()` 的 `direct` 原始键查询精确命中，L2/L3 **完全没有纠正的机会**。
  脏键里含 `擇` 而非 `择` ⇒ 是旧的不完整繁简表时期写进去的（见 `2026-09-12 #8`）。
- **做法（一读一写，缺一不可）**：
  1. **读时复核「更具体者胜」**（`normalizer.ts`）：L1 精确命中后再比一次，若存在一个名字
     ①更**长**（更具体）②**包含**当前名字 ③与文件名相似度 ≥ 0.8 ④比当前命中高出 ≥ 0.03，就采纳更具体的那个。
     两个证据源：别名表官方名 + **归档区里已存在的番剧文件夹**（用户已手工确认过的结果最可靠）。
  2. **写时护栏**（`store.upsertAlias`）：别名**更长且与官方名相似度 ≥ 0.8** 时判为「另一季的标题」**拒收**。
     ⚠️ 这里不能用「字面包含」：繁简与装饰符会让包含失效（`…不擇手段！領主的養女` vs `…〜而不择手段〜`）。
- **实测**：第四季文件对第一季名 **0.822** / 对第四季名 **0.915**（`S4键.includes(S1键)=true`）⇒ 选第四季；
  第一季文件反向 **0.909 / 0.823** ⇒ 不会被抢；已精确命中时相似度≈1.0 ⇒ 天然不可能被抢。
  ⚠️ 只比「谁更相似」不够：`Full Dive` 对 `CLANNAD` 也有 0.179（对正确名字却是 0.000），
  必须有「包含 + 绝对阈值」两道闸（这一点曾把 A5/A7/A8/A13 全部带崩）。
- **数据修复**：备份 `data/aliases.backup-<ts>.json` 后剔除脏别名、删脏键、给正确的第四季条目补键。
  ⚠️ **必须先停服再改文件**，否则服务内存里的旧表会在 `flush()` 时覆盖修复；
  ⚠️ 比对别名要用 `aliasKey(a) === aliasKey(x)`，别用 `===` 字面比较（别名里多一个空格就匹配不到）。
- **验证**：自检 **A37 / A38**；真实扫描日志 `ℹ 已载入 709 个已归档番剧文件夹`，目标路径正确。
- ⚠️ **性能教训**：第一版遍历 3600+ 条键、热循环里现场算键 = **229 ms/名字 ≈ 每次扫描多花 172 s**；
  改成按**官方名**去重（3619 → 721）+ 预计算键 + `WeakMap` 缓存文件夹键 = **0.62 ms/名字（约 1%）**。

---

## 二、媒体库：浏览与展示

### #2 四层折叠，且默认全部折叠

- **现象**：媒体库一进来全铺开 —— 一个年份上千个文件，滚动卡、也难找。
- **做法**：年份 / 月份 / 番剧卡片 / 子目录**四层都默认折叠**，折叠时**不渲染**子树；
  **检索时自动全部展开**；检索期间的点击记在「覆盖」里（优先级高于强制展开），**检索词一变就清空覆盖**
  （否则搜索期间用户点了没反应）。
  ⚠️ 折叠键要加 `lib-` 前缀 —— 归档视图用的是 `y2026`，两边共用 store 里同一张折叠表。
- **验证**（2026 年，Playwright 数 DOM）：初始只渲染 9 个年份行、**245 个 DOM 节点**；
  逐层展开到子目录才 940 个。点子目录行里的按钮**不会**把该行误折叠（`stopPropagation` 有效）。

### #3 文件行按「集数数字」排，不是按文件名字符串

- **现象**：同一部番剧混了两个字幕组（`[BeanSub&FZSD]…` / `[Nekomoe kissaten]…`），
  列表里 01 后面直接跟 14，02 要翻到另一段才出现。
- **原因**：索引（`readdir` 原顺序 = 名字序）与前端渲染都照数组顺序 ⇒ 两个字幕组各自排成一堆。
- **做法**：新增 `compareByEpisode`，**两处都改**（只改一处会不一致）：
  前端（渲染时排，**立即生效、不用重建索引**）+ 服务端索引（`indexer` 收集完 `sort`，下次重建/归档后生效）。
  规则：① 有集数的在前，按数值；② 同集数按后缀（`24` 在 `24v2` 前）；③ 解析不出集数的（NCOP/特典/未识别）**放最后**。
- **验证**：自检 **A39**；实测顶层 `01 02 03 04 06 … 23 24 24v2 25` 递增，
  子目录 `05 05 05 —` 递增。
  ⚠️ 验证时注意：顶层与子目录是**两个容器各自排序**，整串检查递增会误报。

---

## 三、媒体库：多选与批量移动

### #4 多选 + 批量移动

- **做法**：文件行勾选框 + 卡片头 / 子目录行**三态「全选」**（全选 / 部分 / 未选）；
  选择集**跨卡片、跨年份**累积（只存文件路径，不存对象 —— 搬完索引会重载）；
  底部固定条 + **卡片头就地入口**；批量走 `/api/library/move-batch`（**只重建一次索引**，逐条调单文件接口会每搬一条重建一次）。
  搬完自动清空勾选；失败则保留（可直接重试）。
- **验证**：勾 11 个后卡片头出现「↗ 移动选中的 11 个」，弹窗里**正好 11 条**；
  零副作用全链路验证手法＝**目标填成它现在的位置**（接口返回 `unchanged`，磁盘零改动）。

### #5 🔴 「移动」入口同名，导致整个文件夹被搬走

- **现象**：用户勾了 1 个文件、点了「批量移动」，结果**同一文件夹里 24 个文件全被搬走**。
- **原因**：番剧卡片头原本就有「↗ 批量移动」（语义＝移整部），新加的底部条按钮也叫「↗ 批量移动」（语义＝移勾选）。
  同名 + 就近可点 ⇒ 点错。日志（`batch-library-move.jsonl`）可完整还原误移并精确回滚。
- **做法**：**同一视图里多个「移动」入口，文案必须自带范围与数量**（并给就地入口加强调色）：

  | 位置 | 文案 | 作用范围 |
  | --- | --- | --- |
  | 番剧卡片头 | `↗ 整部移动（N）` | 这一部番剧的**全部**文件 |
  | 番剧卡片头（有勾选时才出现） | `↗ 移动选中的 N 个` | **只**这一部里勾选的那些 |
  | 子目录行 | `↗ 移动整个目录` | 那个子目录（含里面所有文件） |
  | 文件行 | `↗ 移动` | **只这一个**文件 |
  | 底部选择条 | `↗ 移动选中 N 个`（强调色） | 跨卡片的**全部**勾选项 |

  ⚠️ 排查这类「勾选没用」的反馈要**先量证据再改代码**：勾 1/24 个后点底部条看弹窗条目数
  （=1 说明选择逻辑没问题，问题在按钮歧义）。
- **误移回滚实操（可复用）**：从 `batch-library-move.jsonl` 精确取 `fromPath`(原)/`toPath`(现) 对
  → 校验每个 `toPath` 仍存在 → `POST /api/library/move-batch`（`fromPaths` + 原 `zhName/year/month`）一次性搬回。
  实测 24 个文件 / 10.32 GB 还原成功。
  ⚠️ 接口响应体是 `{ok,data:{…}}`，读 `$r.moved` 会全是空值，要读 **`$r.data.moved`**。

### #6 入口必须「就地」，只放底部条不够

- **现象**：用户勾选后问「怎么只移动我勾选的那几个」—— 底部浮条没被注意到（截图/视野里常常只有内容区）。
- **做法**：勾选后**卡片头就地多出** `↗ 移动选中的 N 个`（强调色，仅 `selCount>0` 时渲染）；
  列表顶部说明也写明用法；底部条保留作跨卡片汇总入口。
  ⚠️ 底部条用 `createPortal` 挂到 `document.body`（`.panel` 是 `overflow:hidden` 的滚动容器，不再依赖「fixed 不受祖先影响」）。

---

## 四、可靠性：反馈、进度、刷新

### #7 长耗时操作没有反馈 ⇒ 用户重复点击 ⇒ 重复操作

- **现象**：点「应用」后弹窗立刻关，然后默默搬几十秒（NAS 上几 GB）；用户以为没生效 → 再点一次。
- **做法**：**弹窗保持打开**并显示
  「⏳ 正在移动 12 个文件（3.9 GB）…（已用 N 秒，请勿重复点击）」，两个按钮 `disabled`；
  搬完显示「正在刷新媒体库…」再关闭。秒表贯穿整个操作（不分段重置）＝「还活着」的信号。
  单文件「↩ 撤回」/「✏️ 改名」也走全局忙碌遮罩，并在入口 `if (busy) return` 忽略重复点击。
- **验证**：用 `page.addInitScript` 包装 `fetch` 把批量移动拖慢 6 秒 —— 1.5s 时弹窗仍在、提示「已用 1 秒」、
  两个按钮禁用；4.5s 时「已用 4 秒」；结束自动关闭并清空勾选。
  ⚠️ Playwright 的 `page.route` 拦截在这个环境**不生效**（真实请求照发），要用包装 `fetch` 的办法；
  ⚠️ `addInitScript` 每次导航都会重跑 ⇒ 跨导航传状态要用 localStorage。

### #8 索引刷新失败只写日志 ⇒ 列表显示旧数据 ⇒ 用户以为没生效

- **做法**：`loadLibrary()` 失败**自动重试一次**；仍失败 `set({libraryError})` →
  列表顶部红色提示条「⚠ 索引刷新失败（服务可能正忙）」+「↻ 重试」按钮；成功后自动清除。
- **验证**：注入索引读取失败 → 红条出现且可见、有重试按钮；去掉注入后点「重试」→ 红条消失、9 个年份正常载入。

---

## 五、文档结构（本次整理）

### #9 把变更记录从需求文档正文迁出，改为按日期/主题分文件

- **现象**：`需求与设计文档.md` 里越堆越厚（本次之前已把实施期变更全塞进 13.5），
  「打开困难、也不好看」，而且需求基线与后来的改动混在一起，翻起来分不清哪条当前有效。
- **做法**：
  - 新建 `docs/changes/`：**按日期分文件**（一个会话/一批改动一个文件），文件内**按主题分小节**，
    条目编号 `YYYY-MM-DD #n`，索引与规则见 `docs/changes/README.md`；
  - `需求与设计文档.md` 的 13.5 只留**指向本目录的指针**，正文不再堆积；
  - 明确**效力顺序**：`docs/changes/<更晚日期>` > 更早日期 > `需求与设计文档.md` 正文 > `实施提示词.md`；
  - **冲突以更新的为准**，且必须在被取代的那条上显式标注 `⚠️ 已被 <新编号> 取代`（不就地改写历史）。
- **验证**：`docs/changes/README.md`（约定 + 效力顺序 + 索引）、`2026-09-12.md`、`2026-09-13.md`（本文件）；
  需求文档 13.5 收缩为指针；`实施提示词.md` §10 保持「一行一条的坑速查」并指向本目录。
---

## 六、发布准备

### #10 发布到 GitHub 前，把文档与源码里的真实环境信息全部脱敏

- **现象**：准备把仓库推到 GitHub，但源码 / 文档 / 自检脚本里到处是**本机真实环境**：
  NAS 主机名与共享名、项目所在的盘符目录、Windows 用户名、以及「作者自己的媒体库清单」。
  直接推上去等于把家庭网络结构 + 个人观影记录公开。
- **风险点（逐一排查过）**：
  1. `README.md` 与各文档正文里的 `\\<NAS 主机名>\<共享名>\…`；
  2. 文档里 `<盘符>:\<项目目录>`、e2e 注释里的 `C:\Users\<用户名>\AppData\…`；
  3. **最容易漏的两处**：`server/data/`（11.5 MB：`config.json` 带真实归档根、`logs/` 每条都记完整
     文件路径、`aliases.json`、Bangumi 缓存）与 `web/data/` + `web/public/data/`（各 9.9 MB：
     索引里列着所有番剧名与文件名）；以及 `.tools/`（102 MB 便携 Node）；
  4. **自检命令本身**：原本想写的示例扫描命令里直接写了真实主机名 —— 这本身又是一次泄漏。
- **做法**：
  - **统一占位符**：`\\NAS\share\已看`（NAS）、`<项目目录>`（本地项目目录）、`<盘符>:\<挂载点>\…`、
    `<本地临时目录>`（用户名）；只在**事故复盘**里保留结构性描述（UNC 前缀被压成 `\<share>`），不再出现真实值。
  - **`.gitignore` 收紧**：`.tools/`、`node_modules/`、`dist/`、`server/data/`、`web/data/`、
    `web/public/data/`、`web/dist/`、别名备份、`*.tmp`、`.e2e/`、编辑器与本机 OS 垃圾文件。
  - **README 拆分**：把原根目录 `README.md`（使用/运维/构建/自检/工程细节，738 行、满是本机信息）整体
    迁到 `docs/使用与运维.md` 并脱敏；根目录**新写一份对外 `README.md`**（功能、快速开始、关键设计、
    目录结构、自检、文档索引、隐私说明、限制），正文不含任何真实路径。
  - **配图不用真机**：截图取自静态 Demo `demo/index.html`（模拟数据，路径一律 `\\NAS\Media`），
    存为 `docs/images/demo-archive.png` / `demo-library.png`；**绝不**截真实媒体库（会暴露番剧与文件名）。
  - 同步修正所有指向旧 README 的交叉引用（`docs/使用与运维.md` 第 5 章、§11 等）。
- **验证**：
  - 全仓扫描 65 个「会被提交的文件」，检查项＝真实 NAS 主机名 / 真实共享名 / 本机盘符与项目目录 /
    Windows 用户名 / 内网 IP 段：**零命中**；
  - 模拟 `git add`：**65 个文件 / 1.3 MB**，其中**没有** `data/`、`dist/`、`.tools/`、`node_modules/`
    （`server/data` 11.5 MB、`web/data` 9.9 MB、`web/public/data` 9.9 MB、`.tools` 102 MB 均被忽略）；
  - 脱敏后重跑后端自检：**通过 43 / 43**（确认改的只是注释/文档，行为未变）；
  - 发布步骤与「每次 push 前自检」清单写进 `docs/使用与运维.md` §12。

---

## 七、服务生命周期

### #11 关闭网页即关闭本地服务（有任务在跑先二次确认）

- **现象（用户需求）**：本地服务会一直驻留，关掉网页后经常忘记关 → `node` 进程与 `9999` 端口一直占着。
  要求「关掉网页就顺带把服务也关掉」，但**正在归档时**得先拦一下，别不小心把任务打断。
- **原因 / 难点**：这件事**不能**简单地在 `beforeunload` 里发一个 `/api/shutdown`：
  1. **刷新页面（F5 / Ctrl+R）同样会触发卸载** ⇒ 「卸载即关闭」的话用户一刷新服务就没了；
  2. 多标签页：关掉其中一个标签页不该停服务；
  3. 浏览器在卸载阶段会**取消**普通 `fetch` ⇒ 信令只能走 `navigator.sendBeacon`；
  4. 「有任务在跑时二次确认」只能用**浏览器原生**弹窗（JS 改不了 beforeunload 的文案）；
  5. 直接 `process.exit` 会打断**跨设备复制**（NAS 上留半截文件）⇒ 必须先走「停止归档」那套语义。
- **做法**：
  - 新增 **`server/src/core/pageSessions.ts`**（纯逻辑、可自检）：页面会话登记表 + 宽限期定时器。
    最后一个会话注销后先等 **2.5s**；期间 `open()` 到新会话（= 刷新回来了）就取消关闭。
    只认**登记过的** sessionId，未登记的一律忽略（防止别的工具一个请求就把服务关掉）。
  - 新增 **`POST /api/page/open` / `POST /api/page/close`**（`ServerContext.pageSessions` 注入）；
    `/api/health` 顺带回传 `pages` 与 `shutdownOnPageClose`（诊断与自检用）。
  - `server/src/index.ts` 的 `autoExit()`：正在执行归档 → `requestExecuteStop()`（不再取新条目，
    已在搬的先跑完，最多等 30s）→ `store.flush()` → 删 `runtime.json` → `process.exit(0)`。
  - 新增前端 **`web/src/pageLifecycle.ts`**：加载时登记会话（会话 id 只属于**本次页面加载**），
    `beforeunload` 里「有任务在跑 **且** 开关打开」才 `preventDefault()`（= 原生二次确认），
    `pagehide`（`event.persisted` 时跳过 —— 进 bfcache 不算关闭）/ `unload` 时用 `sendBeacon` 发注销信令。
    顺带把归档进度条的文案改成与行为一致（旧文案写的是「关掉页面不影响」）。
  - 配置项 **`shutdownOnPageClose`**（默认 `true`）：设置弹窗新增「④ 关闭网页时自动关闭服务」；
    `false` 时前后端都不动作（回到旧行为：关页面后任务继续在后台跑）。
- **验证**：
  - `server/src/e2e.ts` 新增 **A40**：未登记会话忽略 / 多标签只关一个不关 / 宽限期内重开取消 /
    真关闭只触发一次 / 开关关闭时不关 ⇒ 后端自检 **44 / 44**；
  - HTTP 层（临时 Store + 随机端口，不碰真实服务与配置）9 项全过，含 `text/plain` 的 `sendBeacon` 报文形态、
    `/api/health` 的 `pages`、`GET /api/page/close` → 405；
  - 真实浏览器（临时服务托管真实 `web/dist`）：打开页面 → 服务端 `[open] … pages=1`；
    有假任务时 `beforeunload` 被拦（`defaultPrevented=true`），清掉任务后不再拦；
    刷新页面 → `[close] pending=true` 后立刻 `↩ 已取消自动关闭服务（页面重新打开 / 刷新）`；
    离开页面 → 1.5s 后进程退出；
  - 真实 `index.ts`（临时项目副本 + 独立端口，避免动用户的 9999）：`open/close` 后 **2.5s 自动退出**、
    `runtime.json` 已清理，控制台打印「⏻ 网页已关闭 → 正在关闭本地服务…」。

---

## 八、统计与图表

### #12 统计/图表支持「按月份」（不只有按年份）

- **现象（用户需求）**：媒体库的「📊 统计与图表」只按**年份**分（各年份番剧数量 / 占用空间、年份明细），
  但归档目录本身就是 `{年份}\{月份}\` 两级，按月看不出「当季追番」的分布（如 4 月 / 7 月 / 10 月档）。
- **原因**：`stats` 只算了 `years`；前端的柱状图又是**另外**在组件里按 `library.anime` 现算一遍
  （与 `stats.years` 同源但重复实现），要加一种粒度得改两处。
- **做法**：
  - `server/src/core/indexer.ts` 的 `computeStats()` 同时产出 `years` 与 **`months`**（`MonthStat`：year/month/animeCount/fileCount/totalSize），
    按「年降序 → 月降序」排；月份缺失（手工建的目录）归入 `month: 0`，界面显示 `YYYY-未知`。
    口径与 `years` 一致：**排除特殊目录**、体积**含子目录里的文件**。
  - 前端 `LibraryView.tsx` 的统计面板改成**行数据驱动**：`rows` 由 `stats.years` 或 `stats.months` 映射而来，
    柱状图与「明细」共用同一份 `rows`（数字必然一致，也顺手删掉了原来重复的 byYear 计算）。
  - 工具栏新增粒度切换 `seg#segBucket`（`按年份 | 按月份`），store 新增 `chartBucket`（仅内存，与 `chartMode` 一致）；
    第 6 张卡片随粒度显示「覆盖年份 N 年 / 覆盖月份 N 个月」。
  - 月度标签 `2024-07` 比纯年份宽，新增 `.bar-row .y.wide{width:64px}`（实测渲染宽度 64px，整列对齐）。
  - **兜底**：旧 `library.json` 没有 `stats.months` 时，前端按 `anime` 现算（`useMemo`），
    避免「切到按月份显示暂无数据」被当成功能没做。
- **验证**：
  - 后端自检新增 **A41**（分桶正确 + 年/月降序 + 子目录计入 + 特殊目录排除 + 两种粒度合计一致）⇒ **45 / 45**；
  - 真实索引（709 部 / 8424 文件）在真实浏览器里：按年份 9 行、按月份 **53 行**，两侧**「N 部」求和 = 709 = 番剧总数**；
    前几个月桶 `2026-07:37 部 / 2026-04:41 部 / 2026-01:27 部` 与服务端 `computeStats` 输出逐项一致；
  - 把索引换回**没有 `stats.months` 的旧形态**再跑一遍：53 行、求和 709，
    且每一行的「标签 + 数值 + 明细文案」与带 `months` 的索引**逐行完全相同**（证明兜底口径一致）；
  - `占用空间` 模式下月份标签与体积也正常（`2026-07 → 153 GB`）。

### #13 统计图表：#12 的「按月份」改成「年份一行 + 就地展开月份」

- **现象（用户反馈 #12）**：「年份一行，展开按文件夹名称月份分行就行，**不需要 12 个月都列出**」——
  即不要「整张图切换粒度」这种二选一，而要**同一张图里年、月两层**；也不耍补空月份。
- **原因**：#12 做的是工具栏粒度切换（`按年份 | 按月份`），年月不能同时看到；且行数据是扁平的，
  没有「某年的月份」这层关系。
- **做法**：
  - 新增 `monthsByYear`（年 → **实际存在**的月份）→ 年份模式下把已展开年份的月份行**插在该年份行后面**；
    行对象统一成 `StatsRow{ depth: 0|1, childCount, year }`，柱状图与明细共用同一份。
  - 年份行前面加展开箭头（`.ycaret`，可点击 + tooltip「展开 202X 年的 N 个月份」）；
    **不可展开的行也渲染占位**（否则各列对不齐）。缩进/淡色用 `.bar-row.child`。
  - **默认展开最新一年**（否则用户得先发现箭头才看得到月份）；一旦手动点过，展开状态就由用户接管。
  - 比例尺仍以**年份**为基准（年份 = 其各月之和 ≥ 任何单月），月份柱与年份柱才能互相比。
  - 顺带修复：`按月份` 平铺模式原来复用了 monthRow 的 `depth: 1` ⇒ 平铺时也被套上子行缩进/淡色，
    改为平铺时 `depth: 0`。
- **验证**（真实索引 709 部，浏览器实测）：
  - 默认 14 行 = 9 个年份 + 最新年（2026）的 5 个月；2026 行箭为 `▾`、其余为 `▸`；
  - 2026 各月 37+41+1+1+27 = **107** = 2026 年份行 ✅（与 `computeStats` 输出逐项一致）；
  - 展开 2024（29+27+32+1+27 = **116** ✅，只有 5 个真实月份，无空月）；收起 2026 后行数 23 → 18 ✅；
  - 明细区同步展开同样的行（`2024-10｜29 部 · 415 个文件｜172 GB`）；
  - `按月份` 平铺 53 行、求和 709、无缩进无箭头 ✅；切回按年份会保留展开状态；后端 A41 仍 45 / 45。

---

## 九、点击区域（本次）

### #14 统计图表：点年份行的**任意位置**都能展开（原来只能点左边的小箭头）

- **现象（用户需求）**：年份行要展开月份，必须精准点中左侧那个 12px 的 `▸`；
  用户要求「点整一条都能展开」（截图红框 = 2026 那一整行：箭头 + 标签 + 柱子 + 数值）。
- **原因**：展开事件只挂在 `.ycaret` 这个小 `span` 上，行本身（`div.bar-row`）没有任何点击区域。
- **做法**（`web/src/views/LibraryView.tsx` 的 `LibStatsPanel`）：
  - 抽出三个纯函数：`expandableYear(r)`（哪些行可展开）、`rowCls(r)`、`rowPropsOf(r)`；
    点击事件从箭头**移到整行**，并补 `role="button"` + `tabIndex=0` + Enter/空格（键盘也能用）。
  - 箭头改成**纯展示**（只留 `aria-hidden`）—— ⚠ 箭头再单独绑一次事件会**连点两下 = 看起来没反应**。
  - CSS（`app.css`）：`.bar-row.expandable{cursor:pointer}` + 悬停高亮 + `:focus-visible` 轮廓；
    用 `padding:2px 4px;margin:-2px -4px` 相互抵消，**列宽与行距和改动前完全一致**。
  - 月份子行（`depth===1`）与工具栏「按月份」平铺模式**不可展开**，点击不做任何事。
- **验证**（真实索引 709 部 / 真实浏览器，Playwright）：
  - 点 2025 年份行的**柱状图区域** → 行数 28 → 46、子行 28 条、箭头变 `▾`；
  - 再点同行的**数值区** → 收回 28 行；点月份子行（child）→ 行数不变（无副作用）；
  - 改动后行数与列布局与改动前逐行一致（`padding/margin` 抵消生效）。

### #15 归档整理 / 媒体库：最小单元的文件行**整行可勾选**

- **现象（用户需求）**：勾选必须点中那个 13px 的小方框；用户要求「不可再往下展开的最小单元文件，
  点整条（截图红框）都能勾选」。
- **原因**：勾选框是行内独立的 `input`，行本身没有点击行为；文件行/条目行是最小单元、**不可再展开**
  ⇒ 行上的点击不存在歧义，没必要逼用户精准点小方框。
- **做法**：
  - 媒体库文件行（`LibraryView.FileRowBase`）与归档结果区条目行（`ArchiveView.GroupCard` 的 `.items li`）：
    行 `onClick` = 勾选 / 取消；「正在拖选文本」不算点击（新增 `utils.isTextSelecting()`，复用于两处）。
  - ⚠ **行内按钮与复选框都必须 `stopPropagation`**：按钮不拦 → 点「▶ 播放 / ↩ 撤回 / ↗ 移动」会顺手改勾选；
    **复选框不拦 → 一次点击触发两次 = 看起来「勾不上」**。
  - CSS：`.lib-file.clickable` 与 `.items li:not(.items-more)` 只加手型光标；
    **不设 `user-select:none`**（文件名还要能拖选复制，拖选由 `isTextSelecting()` 兜住）。
  - 媒体库仍保留卡片头「全选本番剧」/ 子目录行「全选该子目录」的三态勾选（那两层可折叠，点击语义是折叠）。
- **验证**（真实浏览器，Playwright 逐项断言 `checked` + 行 `.picked` 类 + 头部「已选 N 项」文案）：
  | 操作 | 结果 |
  | --- | --- |
  | 媒体库点文件名 / 集数徽标 / 修改时间 | ✅ 勾选（`.picked` + 底部批量条出现） |
  | 再点其它空白处 | ✅ 取消 |
  | 点复选框本体两次 | ✅ 勾选 → 取消（各一次，无「双触发」） |
  | 点行内「↗ 移动」 | ✅ 弹出二次整理弹窗，**勾选状态不变** |
  | 归档结果区点文件名 / 大小 / 时间 / 行内空白 | ✅ 勾选 / 取消，头部提示同步为「已选 1 / 2 项」 |
  | 归档结果区点行内「↗」 | ✅ 弹窗打开、勾选不变 |
- 🔎 **无风险验证手法（可复用）**：临时把「源目录」指到一个**本地夹具目录**（2 个 16 B 的假 mp4）
  → 点「↻ 重新扫描」拿到真实卡片与条目行 → 逐项点击断言 → 用 `POST /api/config` 把原配置**写回**，
  再从备份还原 `server/data/last-scan.json`（**只读的操作**：扫描本身不移动任何文件）。
  ⚠ 扫描会 `saveConfig` 覆盖 `sourceRoot/targetRoot`，**测完必须还原并核对**（本次已核对通过：
  界面路径回到 `\\<NAS>\<共享>\已看`，`last-scan.json` 与测试前**逐字节一致**；
  `config.json` 只是把默认值 `shutdownOnPageClose:true` 显式写了出来，行为不变）。
  ⚠ 顺手清掉 `localStorage: anime-archive:last-scan:v1`，否则兜底快照里会留着夹具。

### #16 扫描后没有数据时不再显示「尚未扫描」（按真实原因分档提示）

- **现象（用户反馈）**：点「↻ 扫描并生成方案」扫完，源目录里没有新视频（或已全部归档完成）时，
  左侧「📥 原始文件区」仍旧显示 **「尚未扫描 / 请点击扫描」**，看起来像扫描压根没跑；
  右侧「📂 归档结果区」则一律说「检索条件可能过于严格」——把「没数据」误报成「筛掉了」。
- **原因**：两处空状态都是**一刀切**的写死文案（`!groups.length` 就渲染同一段文字），
  完全没有利用已有的信息：`store.scanInfo`（本次扫描的视频/文件夹/忽略/排除数）与服务端快照 `snapshot`。
- **做法**（`web/src/views/ArchiveView.tsx`，新增组件 `EmptyHint{variant:'source'|'target'}`）：
  - ① `snapshot && snapshot.groups === 0`（上次结果已全部归档完成）→
    「上次扫描的文件已全部归档完成」+ 扫描时间 + 「把新文件放进源目录后再扫」+ 「↻ 扫描并生成方案」按钮；
  - ② `scanInfo.files === 0` → 「**扫描完成，但源目录里没有视频文件**」+ **报出扫描数字**
    （`视频 0 个 / 文件夹 0 个 · 已忽略非视频 N 个 · 已排除归档目录内 K 项`）+
    写明两种可能（已经全部归档过 / 源目录填错）+ 重新扫描入口；
  - ③ `scanInfo.files > 0` 却没分组 → 「扫描完成，但没有可归档的分组」+ 数字；
  - ④ 真的没扫过（`scanInfo` / `snapshot` 都为空）→ 保留原来的引导文案。
  - 右侧「归档结果区 / 归档对照列表」用 `variant='target'` 走同一套判定；
    ⚠ 只有**一个分组都没有**（`groups.length === 0`）时才换文案，被筛选/检索筛掉时仍说「检索条件可能过于严格」
    （为此给 `TargetTree` / `DiffTable` 加了 `emptyWhenNone` 插槽，由 `TargetPanel` 注入）。
- **验证**（真实浏览器，Playwright 逐项读 `innerText`）：
  - 空目录扫描 → 左：「扫描完成，但源目录里没有视频文件 / 扫描结果：视频 0 个 / 文件夹 0 个。…」；
    右：「没有可归档的目标 / 扫描结果：视频 0 个 …」✅
  - 「只有 1 个 .txt」的目录扫描 → 文案追加「· 已忽略非视频 1 个」✅（数字接入正确）
  - 未扫描状态（清掉 `scanInfo`/快照后刷新）→ 仍是「尚未扫描 / 填写上方…」✅
  - 用户真实快照（`files:0 / excluded:1`，即源目录里只剩归档根目录）重新载入 →
    「上次扫描的文件已全部归档完成（09-13 19:43）」✅ —— 正是用户遇到的那个场景
- 🔎 仍用 #15 那套无风险办法验证（夹具目录 + `POST /api/config` + 从备份还原 `last-scan.json`），
  测完核对：界面路径与服务端快照**都回到测试前状态**（`last-scan.json` 内容与备份逐字节一致，
  `savedAt: 2026-09-13T11:43:20Z / sourceRoot: \\<NAS>\<共享>\已看`）。